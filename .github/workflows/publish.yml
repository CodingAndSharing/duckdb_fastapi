name: Publish to PyPI

on:
  push:
    tags:
      - "v*.*.*"  # Triggers only on version tags like v1.0.0

jobs:
  build-and-publish:
    name: Build and Publish to PyPI
    runs-on: ubuntu-latest
    # Uncomment the environment below if you've configured a PyPI trusted publisher environment
    environment: pypi
    permissions:
      id-token: write  # Required for trusted publishing with OIDC
      contents: read

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"
          cache: pip

      - name: Install build dependencies
        run: |
          python -m pip install --upgrade pip
          python -m pip install build wheel twine

      - name: Verify version matches tag
        run: |
          TAG_VERSION=${GITHUB_REF#refs/tags/v}
          PACKAGE_VERSION=$(python -c "import tomllib; print(tomllib.load(open('pyproject.toml', 'rb'))['project']['version'])")
          if [ "$TAG_VERSION" != "$PACKAGE_VERSION" ]; then
            echo "Tag version $TAG_VERSION does not match package version $PACKAGE_VERSION"
            exit 1
          fi

      - name: Build distribution packages
        run: python -m build

      - name: Check built packages
        run: |
          python -m twine check dist/*

      - name: Publish to PyPI
        uses: pypa/gh-action-pypi-publish@release/v1
        with:
          verbose: true
          # Uncomment one of the options below:
          # Option 1: Use trusted publishing (default, requires PyPI configuration)
          # Leave as-is with id-token permissions above
          
          # Option 2: Use PyPI token (add PYPI_API_TOKEN as GitHub secret)
          # password: ${{ secrets.PYPI_API_TOKEN }}
